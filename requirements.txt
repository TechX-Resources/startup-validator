# Startup Idea Validator Agent — minimal dependencies
# Add more as you implement (e.g. openai, chromadb) in later weeks

fastapi
uvicorn[standard]
pydantic
pydantic-settings

# Add these for Azure OpenAI
openai
python-dotenv

# Testing (Week 2+)
pytest
httpx
