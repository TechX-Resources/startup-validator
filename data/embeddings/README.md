# data/embeddings

**Purpose:** Vector representations for ideas or documents — used by the memory/vector store (e.g. Chroma persistence path). Set `VECTOR_DB_PATH` in `.env` to this folder if using file-based storage.

**What students will implement:** Configure Chroma (or similar) to persist here; generate embeddings in Week 4 when implementing memory.

**Weeks:** 4.
