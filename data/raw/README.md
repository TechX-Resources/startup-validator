# data/raw

**Purpose:** Raw data from external sources — e.g. scraped pages, API dumps, search result JSON. Do not commit large or sensitive files; use `.gitignore` for binaries and secrets.

**What students will implement:** Save outputs from web_search, competitor_finder, or market_estimator here during development if you want to cache or inspect; optional.

**Weeks:** 3.
