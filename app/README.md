# app

**Purpose:** Backend application — FastAPI app, config, models, tools, agents, services, memory, schemas, and utils. This is the main codebase for the Startup Idea Validator Agent.

**What students will implement:** See each subfolder’s README. High-level flow: `main.py` → `services` → `agents` (using `models`, `tools`, `memory`) → `schemas` for I/O.

**Weeks:** 1–6 (full roadmap in root README).
